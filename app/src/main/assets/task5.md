#### __Выполнится ли вывод на консоль строки `END.` (Да/Нет)?__


__Подсказка: тип `INTEGER` - это целочисленный 32 разрядный тип.__
```CODE
INTEGER sign = 1; 
WHILE (sign > 0) {
    sign += 1;
}    
PRINT('END.');
```
