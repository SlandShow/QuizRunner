#### Результат работы данной программы – число

```
TMP := 0;
WHILE (TMP < 100) {
    TMP := TMP + P; 
    IF (TMP mod 4 = 0) 
        BREAK;
    PRINT(TMP + " ");
}     
```

#### Наименьшее натуральное значение параметра `P` равно...